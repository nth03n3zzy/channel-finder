import web_scaping.py
from url_list.py import url
import csv


def create_csv(url):

    data = [
        ["date", 'oponnent', 'time', 'channel',]
    ]


for i in URL_list:
    create_csv(URL_list[i])
